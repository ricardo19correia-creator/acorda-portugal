/**
 * 🇵🇹 ACORDA PORTUGAL — COMPATIBILIDADE DE SUÍTE DE TESTES: MULTI-DISPOSITIVO
 * 
 * Reencaminha e valida a nova arquitetura multi-dispositivo sem falsas expulsões:
 * A mesma conta pode estar autenticada em simultâneo em múltiplos dispositivos
 * (PC, Telemóvel, APK), sincronizada em tempo real através do Firebase Auth e Firestore.
 */

import './test_multi_device_sync'
